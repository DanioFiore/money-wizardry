apiVersion: v1
kind: Service
metadata:
  name: money-wizardry-service
  namespace: money-wizardry
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: money-wizardry